import ContactForm from "./ContactForm";

const ContactPage = () => {
  return (
    <div className=" flex items-center justify-center bg-[url('/images/contact-bg.png')] bg-cover bg-center">
      <ContactForm />
    </div>
  );
};

export default ContactPage;
